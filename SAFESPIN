(TURN ON SPINDLE AFTER SAFTEY CHECK)


(#1 is the commanded RPM)

#100 = R_TOOL_DATA[0,99,101] (USE THIS FOR STORAGE)

IF [#100 == 1] (THE PROBE WAS PREVIOUSLY LOADED AND THE SAFTEY BIT IS ON)
	MSG_OK["CHECK FOR PROBE IN SPINDLE", "MAKE SURE YOU HAVE REMOVED THE PROBE FROM THE SPINDLE!!", ""]
	W_TOOL_DATA[0,99,101,0]
ELSE 
	S#1 M3
END_IF

M99